use std::collections::HashMap;

struct Solution {
    _placeholder: i32
}

impl Solution {
    pub fn length_of_longest_substring(s: String) -> i32 {
        let mut char_dict = HashMap::new().reserve(s.len());
        let mut longest_substr: u32 = 0;
    }
}

fn main() {
    println!("Hello, world!");
}
